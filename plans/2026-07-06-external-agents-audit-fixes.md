# План: правки по аудиту «область глазами внешнего агента»

## Контекст

06.07.2026 штаб (Claude, сессия 1fe503) провёл аудит организации
рабочей области mindBox: публичный движок + приватный тенант
`private/usmall-mindbox`. Вторым аудитором выступил реальный Codex
через duet — как «живой» внешний агент на холодном старте (1 раунд).

Главный вывод: для Claude Code область организована хорошо (маршрутные
таблицы CLAUDE.md, триаж summaries.json, карта тенанта), но для
внешнего агента (Codex, Cursor…) она немая — вся навигация живёт в
CLAUDE.md, который чужие агенты по своим конвенциям не читают.

## Утверждённый объём (Павел: «все кроме пункта 2»)

1. **AGENTS.md в оба репо** (движок и тенант): 20–30 строк — что за
   репо, где активный тенант, главные маршруты, какие каталоги
   generated/отсутствуют в свежем клоне (`docs/`, `developers/`,
   `journal/` в .gitignore — скрейпятся локально), безопасные команды
   поиска (`rg -g "*.md" -g "*.yaml"`, не грепать HTML-снапшоты).
   Не дублировать CLAUDE.md — указатель + минимум для холодного старта.
2. ~~fact_index.json / faceted-индексы, обещанные CLAUDE.md, но
   отсутствующие на диске~~ — вынесен отдельной задачей и **выполнен
   2026-07-06** (см. «Пункт 2 — обогащение journal» ниже). План
   2026-05-08-journal-enrichment впервые прогнан целиком на живом
   ключе; journal остаётся в .gitignore, артефакты локальные.
3. **BOOTSTRAP.md**: `private/usmall/` → `private/usmall-mindbox/`
   (реальное имя тенанта) + пояснение про автоопределение активного
   тенанта.
4. **Пометить ссылки, выходящие за границу репо** (`../knowledge/...`,
   `../CLAUDE.md`, `../.claude/behavior/` — CLAUDE.md:255, CLAUDE.md:278,
   notes/customer-filter-grammar.md, notes/customer-profile-extraction.md):
   оговорка «доступно только внутри воркспейса CRM, в отдельном клоне
   ссылка не работает».
5. **Тенант**: снять из админки price-drop пуши в
   `mob_push/transfer_push/` (карточки пар IOS/Android + deeplink) —
   аудит показал, что тексты есть только в YAML сценариев, карточек
   и deeplink нет.
6. **Прогнать /sync-docs** — все корпуса WARN по свежести (26 дней).

## Основания (находки аудита)

- Codex на холодном старте прочитал 4 корневых файла до понимания
  устройства; без CLAUDE.md не нашёл бы активного тенанта,
  `dependencies.md` и правила поиска по journal.
- BOOTSTRAP.md:69 расходился с фактическим именем каталога тенанта.
- Свежий клон движка не содержит корпусов вовсе (осознанный трейдофф
  из-за конфликтов параллельного скрейпа) — но это нигде не сказано
  в точке входа для агента.
- Практическая навигация работает: три тестовых вопроса Codex решил
  за 2–4 чтения каждый (поля письма о заказе; сегмент «Кастомная ГКГ»,
  18 зависимых сценариев; price-drop пуш).

## Не вошло (отвергнуто штабом)

- Вынос routing-таблицы CLAUDE.md в отдельный NAVIGATION.md
  (предложение Codex): второй словарь навигации с риском расползания;
  вместо этого AGENTS.md-указатель.

## Текущий статус на 2026-07-06

Сделано в публичном репо `mindBox`:

- Добавлен корневой `AGENTS.md` для внешних агентов: входная карта,
  активный тенант, generated-корпуса, безопасный поиск и UTF-8 на Windows.
- В `BOOTSTRAP.md` путь приватного тенанта исправлен на
  `private/usmall-mindbox/`, добавлено пояснение про автоопределение
  активного тенанта.
- В `CLAUDE.md`, `notes/customer-filter-grammar.md`,
  `notes/customer-profile-extraction.md` внешние workspace-only ссылки
  помечены как неработающие в отдельном клоне.
- План добавлен в `plans/INDEX.md`.

Сделано в приватном репо `private/usmall-mindbox`:

- Добавлен `AGENTS.md` для холодного старта внешнего агента внутри тенанта:
  README как карта, source-of-truth в Mindbox UI, сценарии `src`/`rendered`,
  `dependencies.md`, секреты, безопасный поиск.

Осталось:

- Снять из админки и положить карточки price-drop push-шаблонов
  (`Price_Decrease_{Cart,Favorite,Viewed}_Trigger_PushIOS/Android_1`)
  с deeplink в `mob_push/transfer_push/` — **отложено командой Павла
  06.07 («пока ничего не снимай»), не начинать без явного запроса**.
- Прогнать `/sync-docs` или эквивалентный sync, потому что корпуса
  `docs/`, `developers/`, `journal` были свежестью около 26 дней.
- ~~Отдельной задачей решить `fact_index.json`/faceted-индексы journal:
  сейчас они упоминаются в старом плане enrichment, но не существуют.~~
  **Сделано 2026-07-06** — см. ниже.

Коммиты не сделаны. У публичного репо и приватного тенанта разные git-статусы;
перед сохранением проверять оба отдельно.

## Пункт 2 — обогащение journal (сделано 2026-07-06)

Решение Павла: жечь OpenAI-ключ (тот же, что у duet-кодекса), а не Anthropic.

**Правки кода (движок `mindBox`, не закоммичены):**

- `scripts/_llm.py` — добавлен OpenAI-бэкенд: structured output через
  function calling с forced `tool_choice`; выбор провайдера через
  `MINDBOX_LLM_PROVIDER` (по умолчанию `anthropic`), ключ
  `OPENAI_API_KEY`, дефолт-модель `gpt-4o-mini`. Общий retry-конверт на
  оба провайдера. Anthropic-путь по умолчанию не изменён.
- `scripts/sync.py` — pre-flight проверка ключа теперь провайдер-aware
  (сообщение обобщено под оба провайдера).
- `requirements.txt` — добавлен `openai>=1.0`.
- `scripts/enrich_journal.py` — исправлен pre-existing баг в `parse_page`
  (`NoneType.append` на frontmatter `tags:` + список), из-за которого
  обогащение падало на реальных данных и ни разу не завершалось.
- `CLAUDE.md` — оговорка «обогащённые артефакты journal только локальны,
  в свежем клоне их нет; сначала `/sync-docs`» + провайдер в описании
  `enrich_journal.py`.

**Прогон (gpt-4o-mini, 2026-07-06):** обе секции, 0 ошибок.

- education: 392/392 `summary_ru` + `key_points`.
- cases: 549/549 `summary_ru` + `key_points` + `fact_index.json`;
  faceted-индексы `by-mechanic` (46), `by-industry` (101), `by-kpi` (102).
- `check_freshness` — все корпуса OK. Живые маршруты из таблицы «Где
  искать ответ» (faceted by-kpi/retention, BM25 `journal_search`) работают.
- Стоимость ≈ $1.07 (оценка: ~1490 вызовов, ~5.0M вход + ~0.5M выход;
  скрипт не логирует точный usage). Wall-clock ~1 ч 55 мин.

**Хвост на будущее (не блокер):** slug'и `industry`/`kpi` в Слое B
свободные → фрагментация faceted-индексов (101 industry, 102 kpi против
46 mechanic на закрытом списке). При желании — нормализовать через
enum/справочник, как уже сделано для mechanics.
