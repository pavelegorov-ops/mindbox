---
title: Как вывести поле товара из сегмента
slug: "как-вывести-поле-товара-из-сегмента"
source_url: "https://help.mindbox.ru/docs/как-вывести-поле-товара-из-сегмента"
vcs_path: "как-вывести-поле-товара-из-сегмента.md"
toc_path:
  - Рассылки
  - "Шаблонизатор, подстановка параметров в рассылки"
  - Параметры для механик. Примеры.
  - Заказы и сегменты
fetched_at: "2026-05-04T11:22:58Z"
content_hash: "sha256:b7c8f8c8603ca23ef958e1c39209ad0d7bafbfb09b140b3b1ee957591b9eb4da"
---

# Как вывести поле товара из сегмента

Задача: подставить в письмо сегмент продуктов.

Например, для запуска еженедельного дайджеста.  
Для начала надо создать **пересчитываемый** сегмент [по фильтру](как-создать-сегмент-по-продуктам-из-фильтра) или [статический](как-создать-статический-сегмент-по-продуктам).  
Чтобы появился [параметр](что-такое-шаблонизатор) для статического сегмента, он должен быть запущен в работу.

#### Как собирается параметр по сегменту продуктов?

Базовый параметр Products + GetBySegment("системное имя сегмента") + Take(<Количество элементов коллекции>)

Всегда надо указывать желамое количество продуктов в коллекции.

В качестве примера в инструкции используем сегмент с системным именем Novinki.

#### Какая коллекция включает 10 продуктов из сегмента?

Products.GetBySegment("Novinki").Take(10)

#### Как вывести данные по каждому продукту?

Выше мы собрали параметр-коллекцию.

Для обращения к каждому элементу в нём используем цикл [for...end for](как-работать-с-элементами-коллекции-с-помощью-цикла-for-end-for-и-set).

По ним можно вывести все поля, представленные на странице:

![image.png](https://storage.yandexcloud.net/assets-help-mindbox-ru/images/image%28109%29.png)

Пример:

```
@{for item in Products.GetBySegment("Novinki").Take(10)}
Название: ${item.Name}
Текущая цена: ${item.Price}
Ссылка на товар: ${item.Url}
@{end for}
```

#### Как вывести продукты в виде [товарной сетки](how-to-display-data-as-a-table) ?

```
@{for row in tableRows (Products.GetBySegment("Novinki").Take(10), 2)}
@{for cell in row.Cells}
@{if cell.Value != null}
Название: ${cell.Value.Name}
Текущая цена: ${cell.value.Price}
Ссылка на товар: ${cell.Value.Url}
@{end if}
@{end for}
@{end for}
```

В новом конструкторе можно настроить вывод сегмента без использования параметров и кода — [инструкция](new-email-builder-product-segment.md).

[Как сегментировать клиентов по жизненному циклу](https://mindbox.ru/academy/education/kak-segmentirovat-klientov-po-zhiznennomu-cziklu/): новички, активные, отток
